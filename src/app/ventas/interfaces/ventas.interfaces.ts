
// Los enum es Asignar palabra o string a valores numericos
// Son como constantes
export enum Color {
    rojo, negro, azul, verde
}

export interface Herore {
    nombre: string;
    vuela: boolean;
    color: Color;
}